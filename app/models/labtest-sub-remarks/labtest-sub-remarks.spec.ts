import { LabtestSubRemarks } from './labtest-sub-remarks';

describe('LabtestSubRemarks', () => {
  it('should create an instance', () => {
    expect(new LabtestSubRemarks()).toBeTruthy();
  });
});
