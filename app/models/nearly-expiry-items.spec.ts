import { NearlyExpiryItems } from './nearly-expiry-items';

describe('NearlyExpiryItems', () => {
  it('should create an instance', () => {
    expect(new NearlyExpiryItems()).toBeTruthy();
  });
});
